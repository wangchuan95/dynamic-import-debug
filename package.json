{
  "name": "vue-cli-dynamic-import-debug",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "build:normal": "cp yarn.lock.bak yarn.lock && yarn && yarn build",
    "build:error": "rm -rf yarn.lock && yarn --registry=https://registry.npmmirror.com && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vue/cli-plugin-babel": "4.5.17",
    "@vue/cli-service": "4.5.17"
  }
}
